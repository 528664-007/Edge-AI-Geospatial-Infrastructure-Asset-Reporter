# Core detection stack
ultralytics>=8.2.0
torch>=2.2.0
torchvision>=0.17.0

# Dashboard
streamlit>=1.36.0
pydeck>=0.9.1
folium>=0.16.0
streamlit-folium>=0.22.0

# Data + persistence
sqlalchemy>=2.0.0
pandas>=2.2.0
numpy>=1.26.0

# Reporting
reportlab>=4.2.0

# Imaging
Pillow>=10.3.0
opencv-python-headless>=4.9.0

# --------------------------------------------------------------------------
# NOTE on GPU acceleration (RTX 3050, 6GB VRAM):
# The line above installs the CPU build of torch/torchvision by default.
# For CUDA acceleration, install the matching CUDA wheel BEFORE the rest of
# this file, e.g. for CUDA 12.1:
#
#   pip install torch torchvision --index-url https://download.pytorch.org/whl/cu121
#   pip install -r requirements.txt
#
# ultralytics will then automatically detect and use the CUDA-enabled torch.
# --------------------------------------------------------------------------
